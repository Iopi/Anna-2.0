package instruction.util;

import instruction.InstructionGenerator;
import instruction.PL0Instruction;
import pl0.type.InstructionType;
import pl0.type.OperationType;

public class RealOperations {

//    static void add(int[] a, int[] b, InstructionGenerator generator) {
//
////        generator.instructions.add(PL0Instruction.builder().instructionType(InstructionType.LIT).value((Integer) expression.getValue().getValue()).build());
//        generator.instructions.add(PL0Instruction.builder().instructionType(
//                InstructionType.OPR).operationType(OperationType.MULTIPLICATION).build()
//        );
//    }

    static void subtract(int[] a, int[] b) {

    }

    static void multiply(int[] a, int[] b) {

    }

    static void divide(int[] a, int[] b) {

    }
}
