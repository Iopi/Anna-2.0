package instruction;

import tree.Declaration;
import type.DataType;

public class DeclarationGenerator {

    public static void generateDeclaration(Declaration declaration, InstructionGenerator generator) {

//        if (declaration.getDataType() == DataType.INT) {
//            ExpressionGenerator.processIntExpression(declaration.getInitialization().getAssignment().getExpression(), generator);
//        } else if (declaration.getDataType() == DataType.REAL) {
//            ExpressionGenerator.processRealExpression(declaration.getInitialization().getAssignment().getExpression(), generator);
//        } else if (declaration.getDataType() == DataType.BOOLEAN) {
//            ExpressionGenerator.processBooleanExpression(declaration.getInitialization().getAssignment().getExpression(), generator);
//        } else {
//            throw new RuntimeException("Not Implemented!");
//        }

        return;
    }
}
