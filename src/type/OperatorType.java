package type;

public enum OperatorType {

    MULT,
    DIVISION,
    PLUS,
    MINUS,
    EQV_EQV,
    NOT_EQV,
    LESS_THAN,
    GREATER_THAN,
    LESS_THAN_OR_EQV,
    GREATER_THAN_OR_EQV,
    AND,
    OR,
    NOT;
}
